; -*- mode: clojure; -*-
; vim: filetype=clojure:ts=2:sw=2

(include "/etc/riemann/helpers")

(let [host "0.0.0.0"]
  (tcp-server {:host host})
  (udp-server {:host host})
  (ws-server  {:host host}))

(periodically-expire 5)

(include "/etc/riemann/streams")

