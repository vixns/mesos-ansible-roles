{
    "cniVersion": "0.2.0",
    "name": "vlan",
    "type": "calico",
    "ipam": {
        "type": "calico-ipam"
    },
    {% if etcd_tls_clients %}
    "etcd_key_file":"/etc/etcd/client.key",
    "etcd_cert_file":"/etc/etcd/client.crt",
    "etcd_ca_cert_file":"/etc/etcd/ca.crt",
    {% endif %}
    "etcd_endpoints": "{% for host in groups['masters'] %}http{% if etcd_tls_clients %}s{% endif %}://{{ hostvars[host].inventory_hostname }}:2379{% if not loop.last %},{% endif %}{% endfor %}"
}